"""Signaling server package for ru-tat-call.

Will host FastAPI REST + WebSocket rooms (invite/accept, SDP, ICE).
No HTTP app in step 0.1 — only the installable package shell.

Example:
    import signaling_server
"""

__version__ = "0.1.0"
