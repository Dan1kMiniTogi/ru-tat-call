"""Signaling server: FastAPI REST (WebSocket rooms in later steps).

Run from `project/`:

    uv run uvicorn signaling_server.app:app --host 0.0.0.0 --port 8000

Demo logins (password `family`): `you`, `mama`, `sister`.
"""

__version__ = "0.1.0"
