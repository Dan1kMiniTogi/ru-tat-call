# apps

Опциональный нативный мобильный клиент (Flutter). Будет разрабатываться post-MVP при необходимости нативных фоновых звонков (CallKit/Push).
Основной клиент для MVP находится в `project/web_client/`.

GPU ASR для Colab: `project/apps/colab_asr/` (шаг 4.1).
