"""Shared library for ru-tat-call (contracts and models).

Pydantic schemas land in step 0.2. Import this package from signaling
and ASR services so REST, WebSocket and ASR events stay in one place.

Example:
    from ru_tat_call_shared import __version__
"""

__version__ = "0.1.0"
