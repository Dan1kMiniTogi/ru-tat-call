[project]
name = "ru-tat-call-shared"
version = "0.1.0"
description = "Shared Pydantic contracts and models for ru-tat-call"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
